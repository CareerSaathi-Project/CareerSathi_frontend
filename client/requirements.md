## Packages
framer-motion | Page transitions and complex animations
recharts | Data visualization for analytics (bar, line, pie charts)
clsx | Conditional class merging
tailwind-merge | resolving tailwind class conflicts

## Notes
- Dark mode is default
- Using framer-motion for all page transitions
- Mock data will be used where API endpoints might not be fully functional yet (though hooks will be set up to use real API)
- Authentication is simulated for now as per instructions (using localStorage/context)
